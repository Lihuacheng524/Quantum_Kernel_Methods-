# 第三章 模型设计

## 3.1 振幅编码量子核方法

振幅编码是一种将经典数据直接映射到量子态振幅上的编码方式。对于一个 $d$ 维的经典向量 $x = (x_0, x_1, \ldots, x_{d-1})$，首先需要对其进行归一化处理，使得向量的 L2 范数等于 1。归一化的具体操作是 $x' = x / \|x\|_2$，其中 $\|x\|_2 = \sqrt{\sum_{i=0}^{d-1} x_i^2}$。如果原始向量的范数为零，则直接返回零向量。归一化完成后，振幅编码将向量映射到一个 $n$ 量子比特系统的量子态上：

$$|\phi(x)\rangle = \sum_{i=0}^{2^n-1} x'_i |i\rangle$$

这里 $n$ 是量子比特数量，系统的希尔伯特空间维度为 $2^n$。当输入向量的维度 $d$ 小于 $2^n$ 时，需要在向量末尾补零使其达到 $2^n$ 维。补零操作使用常数零填充，即 `mode='constant'`。

两个样本 $x_1$ 和 $x_2$ 之间的核函数值定义为它们对应量子态的内积模平方：

$$K(x_1, x_2) = |\langle \phi(x_2) | \phi(x_1) \rangle|^2$$

在实现层面，振幅编码通过 PyQPanda3 量子计算框架完成。对每个归一化后的样本向量，调用 `Encode` 类的 `amplitude_encode` 方法，传入量子比特列表和归一化后的数据向量。该方法构建一个量子电路，其酉矩阵表示可以通过 `get_circuit().matrix()` 获取。对于样本 $x_1$ 和 $x_2$，分别得到酉矩阵 $M_1$ 和 $M_2$。核值的计算方式是先计算 $M_{total} = M_2^\dagger M_1$，然后取 $M_{total}[0, 0]$ 位置的元素，计算其模的平方，即 $|M_{total}[0, 0]|^2$。

这个计算的物理含义是：振幅编码将数据编码到量子态 $|\phi(x)\rangle = U(x)|0\rangle$，其中 $U(x)$ 是编码电路的酉矩阵。两个量子态的内积为 $\langle \phi(x_2) | \phi(x_1) \rangle = \langle 0 | U^\dagger(x_2) U(x_1) | 0 \rangle$，这正是 $U^\dagger(x_2) U(x_1)$ 矩阵左上角的元素。

核矩阵的构建过程如下：对于训练集 $X_1$（包含 $N_1$ 个样本）和测试集 $X_2$（包含 $N_2$ 个样本），需要计算所有 $(N_1 \times N_2)$ 对样本之间的核值。为了加速计算，使用 joblib 的 `Parallel` 工具进行并行化处理。首先生成所有样本对的索引列表 `pairs = [(i, j) for i in range(N1) for j in range(N2)]`，然后使用 `Parallel(n_jobs=-1)` 将所有 CPU 核心投入计算，每个样本对的核值计算是独立的。计算完成后，将结果按索引填充到核矩阵 $K$ 的对应位置。

当训练集和测试集是同一个集合时（即 $N_1 = N_2$），核矩阵需要保证正定性。实现中在对角线上添加了一个小的正则化项 $\epsilon = 10^{-5}$，即 $K \leftarrow K + \epsilon I$。这个操作确保核矩阵是严格正定的，避免后续 SVM 训练时出现数值不稳定问题。

## 3.2 角度编码量子核方法

角度编码使用量子旋转门将经典数据编码到量子电路中。与振幅编码直接将数据放入振幅不同，角度编码将数据值作为旋转门的旋转角度。

角度编码的量子电路由两个部分组成：数据编码部分和参数化演化部分。数据编码部分首先对输入向量进行归一化处理，然后调用 PyQPanda3 的 `angle_encode` 方法，将归一化后的数据映射到量子比特的旋转角度上。具体来说，对每个量子比特 $i$，施加一个 RY 旋转门 $RY(\theta_i)$，其中 $\theta_i$ 是归一化后向量的第 $i$ 个分量。

参数化演化部分由 $L$ 层相同的电路结构堆叠而成，每层包含两个步骤。第一步是纠缠操作：对所有相邻的量子比特对 $(q_i, q_{i+1})$ 施加 CNOT 门，其中 $q_i$ 是控制比特，$q_{i+1}$ 是目标比特。第二步是参数化旋转：对每个量子比特 $q_i$ 施加一个 RY 旋转门 $RY(\theta_{l,i})$，其中 $\theta_{l,i}$ 是第 $l$ 层第 $i$ 个量子比特的可训练参数。

角度编码核的核值计算方式与振幅编码相同，都是通过量子态重叠来计算。对于两个输入样本 $x_1$ 和 $x_2$，分别构建它们对应的完整量子电路（包含数据编码和参数化演化），得到酉矩阵 $M_1$ 和 $M_2$，核值为 $|M_2^\dagger M_1[0, 0]|^2$。

角度编码核的核心特点是引入了可训练参数 $\Theta = \{\theta_{l,i}\}$，这些参数通过优化过程自动学习。优化目标采用核目标对齐（Kernel Target Alignment, KTA）准则。KTA 衡量的是核矩阵 $K$ 与理想标签矩阵 $Y$ 之间的 Frobenius 内积归一化值：

$$\text{KTA}(K, Y) = \frac{\langle K, Y \rangle_F}{\|K\|_F \|Y\|_F} = \frac{\sum_{i,j} K_{ij} Y_{ij}}{\sqrt{\sum_{i,j} K_{ij}^2} \sqrt{\sum_{i,j} Y_{ij}^2}}$$

标签矩阵 $Y$ 的构造方式是：$Y_{ij} = 1$ 当样本 $i$ 和样本 $j$ 属于同一类别，否则 $Y_{ij} = 0$。在代码中通过 `np.equal.outer(y_support, y_support).astype(float)` 实现。

优化过程使用 scipy 的 `minimize` 函数，选择 L-BFGS-B 优化算法。初始参数从 $[-\pi/4, \pi/4]$ 的均匀分布中随机采样，随机种子固定为 0 以保证可重复性。参数矩阵的形状为 $(L, n)$，其中 $L$ 是层数（默认为 2），$n$ 是量子比特数量。优化时将参数矩阵展平为一维向量传入优化器，优化完成后重新 reshape 为矩阵形式。最大迭代次数设置为 20 次。

优化过程中，每次迭代都需要重新计算整个支持集的核矩阵。对于 $N$ 个样本的支持集，核矩阵是对称的，因此只需要计算上三角部分，然后对称填充到下三角。损失函数定义为负的 KTA 值，即最小化负 KTA 等价于最大化 KTA。

核矩阵构建时同样添加了对角正则化项 $\epsilon = 10^{-5}$。

## 3.3 ZZ 重叠量子核方法

ZZ 重叠量子核使用 ZZ Feature Map 进行数据编码。ZZ Feature Map 是一种常用的量子特征映射方法，其电路结构包含单量子比特旋转门和两量子比特纠缠门。

编码过程分为三个阶段。第一阶段是对每个量子比特施加 Hadamard 门，将初始态 $|0\rangle^{\otimes n}$ 转换为均匀叠加态 $\frac{1}{\sqrt{2^n}}\sum_{x=0}^{2^n-1}|x\rangle$。第二阶段是对每个量子比特施加 RZ 旋转门，旋转角度为 $2x_i$，其中 $x_i$ 是输入向量的第 $i$ 个分量。第三阶段是对所有量子比特对 $(i, j)$（其中 $i < j$）施加 CRZ 纠缠门，纠缠强度为 $2(\pi - x_i)(\pi - x_j)$。CRZ 门是受控 RZ 门，当控制比特为 $|1\rangle$ 时对目标比特施加 RZ 旋转。

ZZ 重叠核的核值计算通过构建一个干涉电路来实现。整个电路的执行流程如下：

首先，对所有 $n$ 个量子比特施加 Hadamard 门，创建叠加态。然后，对第一个样本 $x_1$ 进行编码：对每个量子比特 $i$ 施加 RZ 旋转门 $RZ(2x_{1,i})$，然后对所有量子比特对 $(i, j)$ 施加 CRZ 纠缠门 $CRZ(2(\pi - x_{1,i})(\pi - x_{1,j}))$。接着，对第二个样本 $x_2$ 进行逆编码：先对所有量子比特对 $(i, j)$ 施加 CRZ 门的共轭转置 $CRZ^\dagger(2(\pi - x_{2,i})(\pi - x_{2,j}))$，然后对每个量子比特 $i$ 施加 RZ 门的共轭转置 $RZ^\dagger(2x_{2,i})$。最后，对所有量子比特施加 Hadamard 门的逆操作（Hadamard 门是自逆的，所以仍然是 Hadamard 门）。

以 $N=4$ 量子比特为例，取两个样本 $x_1 = [0.1, 0.2, 0.3, 0.4]$ 和 $x_2 = [0.15, 0.25, 0.35, 0.45]$，电路的门序列如下：

```
================================================================================
ZZ Overlap Kernel Interference Circuit (N=4 qubits)
================================================================================

[Stage 1] Initialize superposition + encode x1 (RZ rotations)
----------------------------------------------------------------------
  Gate   1: H on q0
  Gate   2: RZ(2*x1[0]) = RZ(0.2000) on q0
  Gate   3: H on q1
  Gate   4: RZ(2*x1[1]) = RZ(0.4000) on q1
  Gate   5: H on q2
  Gate   6: RZ(2*x1[2]) = RZ(0.6000) on q2
  Gate   7: H on q3
  Gate   8: RZ(2*x1[3]) = RZ(0.8000) on q3

[Stage 2] Encode x1 entanglement (CRZ gates)
----------------------------------------------------------------------
  Gate   9: CRZ(17.8943) on control=q0, target=q1
  Gate  10: CRZ(17.2859) on control=q0, target=q2
  Gate  11: CRZ(16.6776) on control=q0, target=q3
  Gate  12: CRZ(16.7176) on control=q1, target=q2
  Gate  13: CRZ(16.1293) on control=q1, target=q3
  Gate  14: CRZ(15.5810) on control=q2, target=q3

[Stage 3] Inverse encode x2 entanglement (CRZ dagger)
----------------------------------------------------------------------
  Gate  15: CRZ^dag(17.3009) on control=q0, target=q1
  Gate  16: CRZ^dag(16.7026) on control=q0, target=q2
  Gate  17: CRZ^dag(16.1043) on control=q0, target=q3
  Gate  18: CRZ^dag(16.1443) on control=q1, target=q2
  Gate  19: CRZ^dag(15.5660) on control=q1, target=q3
  Gate  20: CRZ^dag(15.0277) on control=q2, target=q3

[Stage 4] Inverse encode x2 (RZ dagger + Hadamard)
----------------------------------------------------------------------
  Gate  21: RZ^dag(2*x2[0]) = RZ^dag(0.3000) on q0
  Gate  22: H^dag(q0) = H(q0)
  Gate  23: RZ^dag(2*x2[1]) = RZ^dag(0.5000) on q1
  Gate  24: H^dag(q1) = H(q1)
  Gate  25: RZ^dag(2*x2[2]) = RZ^dag(0.7000) on q2
  Gate  26: H^dag(q2) = H(q2)
  Gate  27: RZ^dag(2*x2[3]) = RZ^dag(0.9000) on q3
  Gate  28: H^dag(q3) = H(q3)

================================================================================
Total gates: 28
  - Hadamard gates: 8
  - RZ gates: 4
  - CRZ gates: 6
  - RZ^dag gates: 4
  - CRZ^dag gates: 6
================================================================================
```

对应的量子电路结构可以用 Qcircuit 表示如下：

```latex
\begin{equation*}
\Qcircuit @C=1.0em @R=1.2em {
  \lstick{\ket{0}_{0}} & \gate{H} & \gate{RZ(2x_1^{0})} & \ctrl{1} & \ctrl{2} & \ctrl{3} & \qw & \ctrl{1} & \ctrl{2} & \ctrl{3} & \gate{RZ^\dag(2x_2^{0})} & \gate{H} & \qw \\
  \lstick{\ket{0}_{1}} & \gate{H} & \gate{RZ(2x_1^{1})} & \targ & \targ & \qw & \targ & \targ & \gate{RZ^\dag(2x_2^{1})} & \gate{H} & \qw \\
  \lstick{\ket{0}_{2}} & \gate{H} & \gate{RZ(2x_1^{2})} & \targ & \qw & \targ & \gate{RZ^\dag(2x_2^{2})} & \gate{H} & \qw \\
  \lstick{\ket{0}_{3}} & \gate{H} & \gate{RZ(2x_1^{3})} & \qw & \gate{RZ^\dag(2x_2^{3})} & \gate{H} & \qw \\
}
\end{equation*}
```

电路执行完成后，得到最终的量子态 $|\psi\rangle$。定义投影算符 $P_0 = |0\rangle\langle 0|^{\otimes n}$，即投影到全零态的算符。在代码中，这个投影算符通过创建一个 $2^n \times 2^n$ 的零矩阵，并将左上角元素设为 1 来实现。核值计算为 $\langle \psi | P_0 | \psi \rangle$ 的实部。

这个设计的物理含义是：如果两个样本 $x_1$ 和 $x_2$ 完全相同，那么编码和逆编码操作相互抵消，整个电路等效于恒等变换，最终态回到 $|0\rangle^{\otimes n}$，测量结果为全零态的概率为 1。如果两个样本不同，编码和逆编码不能完全抵消，最终态与初始态存在偏差，测量概率降低。核值的大小反映了两个样本的相似程度。

核矩阵的构建采用双重循环遍历所有样本对，对每对样本调用 `kernel` 函数计算核值。这种方法没有使用并行化，计算复杂度为 $O(N_1 \times N_2)$。

## 3.4 ZZ 投影量子核方法

ZZ 投影量子核同样使用 ZZ Feature Map 进行数据编码，但核函数的计算方式与 ZZ 重叠核完全不同。它采用了一种"先测量后计算"的策略，将量子测量结果作为经典特征向量，然后在经典空间中计算高斯核。

数据编码部分使用 PyVQNet 框架中的 `VQC_ZZFeatureMap`。这个函数接收输入特征向量、量子机器实例、数据映射函数和纠缠方式作为参数。数据映射函数 `custom_data_map_func` 的定义是：当输入向量维度为 1 时，直接返回该值；否则计算向量所有元素的乘积。纠缠方式设置为 "linear"，即相邻量子比特之间进行 ZZ 纠缠。

编码完成后，对量子态进行测量。测量操作使用 `MeasureAll` 类，分别测量所有量子比特在 X、Y、Z 三个泡利基下的期望值。具体来说，对每个量子比特 $i$，构造可观测量 $X_i$、$Y_i$、$Z_i$（分别对应泡利 X、Y、Z 矩阵作用在第 $i$ 个量子比特上），然后计算每个可观测量的期望值。测量结果是一个 $3n$ 维的实值向量，前 $n$ 个分量是 X 基下的期望值，中间 $n$ 个分量是 Y 基下的期望值，最后 $n$ 个分量是 Z 基下的期望值。

对于训练集中的每个样本和测试集中的每个样本，都执行上述编码和测量过程，得到各自的 $3n$ 维特征向量。然后，对于任意两个样本的特征向量 $v_1$ 和 $v_2$，计算它们之间的欧氏距离平方：

$$d^2(v_1, v_2) = \sum_{k=1}^{3n} (v_{1,k} - v_{2,k})^2$$

核值通过高斯核函数计算：

$$K(x_1, x_2) = \exp(-\gamma \cdot d^2(v_1, v_2))$$

其中 $\gamma$ 是带宽参数，在代码中通过 `params` 参数传入，默认值为 1.0。

核矩阵的构建同样采用双重循环，但使用了 PyVQNet 的张量运算。首先对训练集和测试集的所有样本进行编码和测量，得到特征向量列表。然后遍历所有样本对，计算特征向量差值的平方和，取指数得到核值。计算过程中使用 PyVQNet 的 `tensor` 模块进行张量运算，确保计算在框架内完成。

## 3.5 量子卷积神经网络

量子卷积神经网络（QCNN）的结构借鉴了经典卷积神经网络的设计理念，包含卷积层、池化层和全连接层，所有操作都在量子电路上执行。网络使用 6 个量子比特。

### 3.5.1 数据编码

QCNN 使用振幅编码将输入数据编码到量子态上。编码通过 `VQC_AmplitudeEmbedding` 函数完成，该函数接收输入特征向量和量子机器实例，将输入数据映射到量子态的振幅上。

### 3.5.2 卷积层

卷积层使用 Ising 相互作用门和 U3 通用单量子门构建。电路对量子比特进行配对处理，采用交错配对策略。

具体来说，对于 $n$ 个量子比特，卷积层分两个子步骤执行。第一个子步骤处理偶数索引的量子比特对 $(w_0, w_1), (w_2, w_3), \ldots$，第二个子步骤处理奇数索引的量子比特对 $(w_1, w_2), (w_3, w_4), \ldots$。

对于每对量子比特 $(w_i, w_{i+1})$，卷积层执行以下操作：首先施加三个 Ising 纠缠门 IsingXX($\theta_6$)、IsingYY($\theta_7$)、IsingZZ($\theta_8$)，这三个门分别在 X、Y、Z 方向上施加两量子比特相互作用。然后对两个量子比特分别施加 U3 门。U3 门是包含三个参数的通用单量子旋转门，形式为 $U3(\theta, \phi, \lambda)$。第一个量子比特的 U3 门参数为 $\theta_9, \theta_{10}, \theta_{11}$，第二个量子比特的 U3 门参数为 $\theta_{12}, \theta_{13}, \theta_{14}$。

需要注意的是，第一层的第一个子步骤会跳过 U3 门的初始旋转（通过 `skip_first_layer` 参数控制），因为振幅编码已经对量子态进行了初始化，不需要额外的单量子比特旋转。

卷积层的参数总数为 15 个：3 个用于 Ising 门，6 个用于第一个 U3 门，6 个用于第二个 U3 门。

### 3.5.3 池化层

池化层使用 CNOT 门和 U3 门实现信息压缩。池化操作对奇数索引的量子比特对进行处理：对于量子比特对 $(w_{2k-1}, w_{2k-2})$，先施加 CNOT 门，其中 $w_{2k-1}$ 是控制比特，$w_{2k-2}$ 是目标比特，然后对目标比特 $w_{2k-2}$ 施加一个 U3 门。

池化层的效果是将有效量子比特数量减半。例如，6 个量子比特经过一次池化后，有效量子比特数量变为 3 个。这是因为 CNOT 门将控制比特的信息转移到了目标比特上，后续操作只关注目标比特。

### 3.5.4 卷积-池化组合

`conv_and_pooling` 函数将卷积层和池化层组合在一起。卷积层使用 15 个参数（索引 0-14），池化层使用 3 个参数（索引 15-17），总共 18 个参数。

网络中交替执行多次卷积-池化操作。每次操作后，有效量子比特数量减半。对于 6 个量子比特，经过一次池化后变为 3 个，再经过一次池化后变为 2 个（向下取整）。

### 3.5.5 全连接层

全连接层使用多种 Ising 旋转门的组合。对于剩余的两个量子比特，施加以下门操作：RZZ($\theta_0$)、RXX($\theta_1$)、RYY($\theta_2$)、RZX($\theta_3$)、RXX($\theta_5$)、RZX($\theta_6$)、RZZ($\theta_7$)、RYY($\theta_8$)、RZZ($\theta_9$)、RXX($\theta_{10}$)、RZX($\theta_{11}$)、RZX($\theta_{12}$)、RZZ($\theta_{13}$)、RYY($\theta_{14}$)。

全连接层的参数总数为 15 个。代码中有一个断言检查：`last_layer_weights.size == 4^(len(wires)) - 1`，对于 2 个量子比特，$4^2 - 1 = 15$，与参数数量一致。

### 3.5.6 网络结构与训练

网络的完整结构是：振幅编码输入 → 卷积-池化层（第 1 层）→ 卷积-池化层（第 2 层）→ 全连接层 → 测量输出。

网络使用 PyVQNet 框架的 `Module` 类定义，继承关系为 `Qcnn_ising(Module)`。模型参数使用 `Parameter` 类定义，包括卷积层权重 `weights`（形状为 $(18, 2)$，数据类型为 float64）和全连接层权重 `weights_last`（形状为 $(15, 1)$，数据类型为 float64）。

前向传播时，首先重置量子机器状态，然后调用 `conv_net` 函数执行完整的量子电路，最后测量第一个量子比特的概率分布。测量使用 `probs` 函数，返回第一个量子比特处于 $|0\rangle$ 和 $|1\rangle$ 态的概率。

模型训练使用 Adam 优化器，学习率设为 0.01。损失函数定义为：

$$L = 1 - \frac{1}{N} \sum_{i=1}^N p_i(y_i)$$

其中 $p_i(y_i)$ 是模型对样本 $i$ 预测其真实类别 $y_i$ 的概率。这个损失函数鼓励模型对正确类别的预测概率趋近于 1。

训练过程中，每个 epoch 执行以下步骤：清零梯度、前向传播计算损失、反向传播计算梯度、更新参数。训练完成后，分别计算训练集和测试集的准确率。准确率的计算方式是：对于每个样本，如果模型对其真实类别的预测概率大于 0.5，则认为预测正确。

## 3.6 原型网络

原型网络作为经典基线方法，其原理是计算每个类别在特征空间中的原型向量，然后根据测试样本到各个原型的距离进行分类。

原型向量的计算方式是：对于训练集中的每个类别 $c$，收集所有属于该类别的样本 $\{x_i | y_i = c\}$，计算它们的均值向量：

$$p_c = \frac{1}{|S_c|} \sum_{i: y_i = c} x_i$$

其中 $S_c$ 是类别 $c$ 的样本集合，$|S_c|$ 是该类别的样本数量。

对于测试集中的每个样本 $x$，计算它到所有原型向量的欧氏距离：

$$d_c(x) = \|x - p_c\|_2 = \sqrt{\sum_{j=1}^d (x_j - p_{c,j})^2}$$

在代码中，距离计算使用 `np.linalg.norm` 函数，通过广播机制一次性计算所有测试样本到所有原型的距离。具体操作是：将测试集 $X_{test}$ 扩展为形状 $(N_{test}, 1, d)$，将原型矩阵 $P$ 扩展为形状 $(1, C, d)$，然后计算差的 L2 范数，得到形状为 $(N_{test}, C)$ 的距离矩阵。

分类规则是将样本分配到距离最近的类别：

$$\hat{y} = \arg\min_{c} d_c(x)$$

在代码中通过 `test_dists.argmin(axis=1)` 实现。

原型网络同时计算训练集和测试集的准确率。训练集准确率的计算方式与测试集相同，只是将测试集替换为训练集。准确率使用 sklearn 的 `accuracy_score` 函数计算。

原型网络不需要训练过程，直接使用训练集计算原型，然后在测试集上进行预测。这种方法简单直接，在小样本场景下有一定的效果。但其特征空间是原始输入空间（经过 PCA 降维后），没有经过量子特征映射的增强。

---

# 第四章 实验设计与结果分析

## 4.1 实验环境

实验在 Linux 操作系统上运行。CPU 采用多核并行计算策略，量子核函数的 Gram 矩阵计算使用 joblib 库的 `Parallel` 工具，设置 `n_jobs=-1` 参数调用所有可用的 CPU 核心。

量子电路模拟使用两个框架：PyQPanda3 和 PyVQNet。PyQPanda3 用于振幅编码核和角度编码核的量子电路构建与核值计算，主要使用其 `Encode` 类进行振幅编码和角度编码，使用 `QCircuit` 类构建量子电路，使用 `CNOT`、`RY` 等门操作构建参数化电路。PyVQNet 用于 ZZ 重叠核、ZZ 投影核和量子卷积神经网络的实现，主要使用其 `QMachine` 类进行量子态模拟，使用 `VQC_ZZFeatureMap` 进行 ZZ 特征映射，使用 `MeasureAll` 进行可观测量测量，使用 `Module`、`Parameter`、`QTensor` 等类构建和训练量子神经网络。

深度学习相关操作使用 PyTorch 框架，主要用于 Omniglot 数据集的加载和预处理。数据处理使用 scikit-learn 库，包括 PCA 降维、StandardScaler 和 MinMaxScaler 数据标准化、SVM 分类器、数据集加载和准确率计算。可视化使用 matplotlib、seaborn 和 pandas 库。

## 4.2 数据集

### 4.2.1 MNIST Digits 数据集

MNIST Digits 数据集使用 scikit-learn 内置的 `load_digits` 函数加载。这个数据集是原始 MNIST 数据集的缩小版本，包含 1797 个 8×8 像素的手写数字图像，每个像素值是 0 到 16 之间的整数。数据集包含 0 到 9 共 10 个数字类别。

实验中使用的是二分类设置，只保留数字 0 和数字 1 两个类别。筛选后得到 360 个样本，其中数字 0 有 178 个样本，数字 1 有 182 个样本。每个样本是 64 维的特征向量（8×8 像素展平）。

数据预处理包括 L2 归一化：对每个样本向量 $x$，计算 $x' = x / \|x\|_2$，使每个样本的 L2 范数为 1。归一化操作在 `load_mnist_data` 函数中完成。

### 4.2.2 Omniglot 数据集

Omniglot 数据集是一个手写字符识别数据集，包含 50 个不同书写系统的 1623 个不同字符，每个字符由 20 个人手写。实验中使用的是 PyTorch 的 `torchvision.datasets.Omniglot` 类加载数据。

数据预处理流程如下：首先将所有字符图像缩放到 28×28 像素，然后转换为灰度图（单通道），接着转换为张量并展平为 784 维向量。训练集使用 background=True 的字符（964 个字符），测试集使用 background=False 的字符（659 个字符）。

实验采用二分类设置：从所有类别中随机选择 2 个类别，筛选出属于这两个类别的所有样本，然后将标签映射为 0 和 1。类别选择使用 `np.random.default_rng(seed=0)` 固定随机种子为 0，保证实验可重复性。

数据预处理同样包括 L2 归一化，使每个样本的 L2 范数为 1。

## 4.3 实验设计

### 4.3.1 实验配置

实验使用统一的配置类 `Config` 管理所有超参数：

- 随机种子 `SEED = 0`
- 量子卷积神经网络训练轮数 `N_EPOCHS = 10`
- 重复实验次数 `N_REPS = 10`
- 测试集大小 `N_TEST = 10`
- 量子比特数量 `N_QUBITS = 4`
- 核矩阵正则化项 `PSD_REG = 1e-5`
- 角度编码层数 `ANGLE_N_LAYERS = 2`
- 原型网络类别数 `N_WAY = 5`
- 原型网络查询样本数 `N_QUERY = 15`
- 原型网络回合数 `N_EPISODES = 100`

### 4.3.2 训练集大小设置

实验设置了 5 个不同的训练集大小：2、5、10、20、40 个样本。这个设置覆盖了从小样本到相对充足样本的范围，可以观察模型在不同数据量下的表现变化。

测试集大小固定为 10 个样本。每个训练集大小配置下，重复实验 10 次，每次使用不同的随机训练集和测试集划分，最终结果取 10 次实验的平均值和标准差。

### 4.3.3 数据预处理流程

对于量子核方法（振幅编码核、角度编码核、ZZ 重叠核、ZZ 投影核），数据预处理包含两个步骤：

第一步是 PCA 降维。使用 scikit-learn 的 `PCA` 类，将数据降维到 `n_components = min(N_QUBITS, x_train.shape[0], x_train.shape[1])` 维。对于 4 量子比特设置，降维到 4 维。PCA 拟合只在训练集上进行，然后对训练集和测试集分别进行变换。

第二步是 MinMaxScaler 缩放。将训练集和测试集拼接在一起，拟合 MinMaxScaler，将数据缩放到 $[-1, 1]$ 区间。缩放操作对训练集和测试集分别进行。

对于量子卷积神经网络，数据预处理只包含 PCA 降维，不进行 MinMaxScaler 缩放。这是因为 QCNN 使用振幅编码，输入需要是非负的（振幅编码要求输入向量元素非负或经过特殊处理）。

对于原型网络，数据预处理与量子核方法相同，包含 PCA 降维和 MinMaxScaler 缩放。

### 4.3.4 训练集划分策略

训练集和测试集的划分使用 `train_test_split_for_size` 函数实现。划分策略考虑了类别平衡：

首先找出所有类别 0 的样本索引和类别 1 的样本索引。然后从类别 0 中随机选择 `n_train // 2` 个样本，从类别 1 中随机选择 `n_train // 2` 个样本，合并作为训练集。如果某个类别的样本数量不足，则选择该类别的所有样本。训练集索引确定后，从剩余样本中随机选择 `n_test` 个样本作为测试集。

这种划分方式保证了训练集中两个类别的样本数量大致相等，避免了类别不平衡问题。

### 4.3.5 各模型实验流程

**振幅编码核**：对预处理后的训练集和测试集，调用 `build_amplitude_kernel` 函数计算核矩阵。训练集核矩阵 $K_{train}$ 的形状为 $(N_{train}, N_{train})$，测试集核矩阵 $K_{test}$ 的形状为 $(N_{test}, N_{train})$。然后使用 SVM 分类器（`SVC(kernel="precomputed")`）进行训练和预测。训练集上使用 `svm.fit(K_train, y_train)` 拟合模型，测试集上使用 `svm.predict(K_test)` 进行预测。

**角度编码核**：首先调用 `optimize_angle_params` 函数在训练集上优化角度编码参数。优化完成后，使用最优参数调用 `build_angle_kernel` 函数计算核矩阵。核矩阵计算和 SVM 训练流程与振幅编码核相同。

**ZZ 重叠核**：对预处理后的训练集和测试集，调用 `vqnet_quantum_kernel` 函数计算核矩阵。核矩阵计算和 SVM 训练流程与振幅编码核相同。

**ZZ 投影核**：对预处理后的训练集和测试集，调用 `vqnet_projected_quantum_kernel` 函数计算核矩阵。该函数返回的是 PyVQNet 的 `QTensor` 对象，需要调用 `.to_numpy()` 转换为 NumPy 数组。核矩阵计算和 SVM 训练流程与振幅编码核相同。

**量子卷积神经网络**：对预处理后的训练集和测试集，调用 `train_test_qcnn` 函数进行训练和测试。该函数内部完成模型初始化、训练和评估的全部流程。训练轮数设置为 10 个 epoch。

**原型网络**：对预处理后的训练集和测试集，调用 `train_test_protonet` 函数进行原型计算和分类。该函数内部完成原型向量计算、距离计算和分类的全部流程。

### 4.3.6 实验执行流程

实验的主函数 `main` 按以下流程执行：

首先定义要使用的数据集列表：`["omniglot", "mnist"]`。然后定义每个数据集上运行的模型列表：MNIST 数据集运行 6 种模型（amplitude、qcnn、zz_overlap、zz_projected、angle、protonet），Omniglot 数据集运行 5 种模型（angle、amplitude、zz_overlap、zz_projected、protonet），不包含 QCNN。

对于每个数据集和模型的组合，遍历 5 个训练集大小，调用 `run_experiment` 函数执行实验。`run_experiment` 函数内部重复 10 次实验，每次随机划分训练集和测试集，运行模型，记录结果。

所有实验结果收集到一个列表中，然后转换为 pandas DataFrame，保存为 `experiment_results.csv` 文件。

### 4.3.7 结果可视化

实验结果可视化使用 matplotlib 绘制性能对比图。每种模型使用不同的线型和颜色：

- 振幅编码核：虚线（D--），橙色（#ff7f0e）
- 量子卷积神经网络：实线圆点（o-），红色（#d62728）
- ZZ 重叠核：虚线方块（s--），蓝色（#1f77b4）
- ZZ 投影核：点划线三角（^-.），绿色（#2ca02c）
- 角度编码核：点线倒三角（v:），紫色（#9467bd）
- 原型网络：实线叉号（x-），棕色（#8c564b）

图表设置：横轴为训练集大小，使用对数坐标（`set_xscale("log")`），刻度为 [2, 5, 10, 20, 40]。纵轴为测试准确率，范围为 [0.3, 1.05]。图中包含一条 y=0.5 的水平虚线，表示随机猜测的准确率。误差线表示 10 次实验的标准差，使用 `errorbar` 函数绘制，误差线帽宽为 5，线宽为 1.5。

图表保存为 PNG 格式，分辨率为 200 DPI，使用 `bbox_inches="tight"` 确保图例和标签不被裁剪。

### 4.3.8 结果表格输出

实验结果同时以 LaTeX 表格形式输出。表格的列是各个模型，行是不同训练集大小。每个单元格显示均值±标准差，格式为 `0.xxx±0.xxx`。

表格使用 `tabular` 环境，列数为模型数量加 1（训练集大小列）。表头为 "Training Size" 和各模型名称。表格内容按训练集大小从小到大排列。

## 4.4 实验结果分析

### 4.4.1 MNIST 数据集结果

在 MNIST Digits 数据集上，6 种模型参与了比较。实验覆盖了从 2 个样本到 40 个样本的训练集大小范围。

振幅编码核将 4 维输入数据映射到 16 维的希尔伯特空间。振幅编码的特点是编码效率高，4 个量子比特可以表示 16 维的向量。核值计算是精确的量子态重叠，没有近似。在训练集较小时，振幅编码核的表现取决于数据在量子特征空间中的可分性。随着训练集增大，SVM 能够更好地学习决策边界。

角度编码核通过可训练参数调整特征映射。KTA 优化使核矩阵更好地对齐标签结构，即同类样本的核值更大，异类样本的核值更小。角度编码使用 4 个量子比特，电路深度为 2 层，每层包含 CNOT 纠缠门和可学习的 RY 旋转门。可训练参数的数量为 $2 \times 4 = 8$ 个。优化过程使用 L-BFGS-B 算法，最大迭代 20 次。在训练集较小时，KTA 优化可能过拟合，导致泛化能力下降。随着训练集增大，优化效果更加稳定。

ZZ 重叠核使用 ZZ Feature Map 进行数据编码，通过量子干涉直接测量两个样本的重叠。编码电路包含 Hadamard 门、RZ 旋转门和 CRZ 纠缠门。核值计算通过构建干涉电路，测量全零态的概率。ZZ 重叠核的特点是核值计算完全在量子电路上完成，没有经典后处理。

ZZ 投影核同样使用 ZZ Feature Map 编码，但核值计算分为两个阶段。量子阶段提取每个样本在 X、Y、Z 三个泡利基下的期望值，得到 12 维特征向量（4 个量子比特 × 3 个基）。经典阶段使用高斯核函数计算核值，带宽参数 $\gamma = 1.0$。ZZ 投影核的特点是结合了量子特征提取和经典核计算，可能在某些场景下具有更好的灵活性。

量子卷积神经网络使用 6 个量子比特，包含 2 层卷积-池化操作和 1 层全连接层。模型参数总数为 $18 \times 2 + 15 = 51$ 个。训练使用 Adam 优化器，学习率 0.01，训练 10 个 epoch。QCNN 的特点是端到端训练，卷积层和池化层自动学习特征提取方式。但在小样本场景下，51 个参数可能导致过拟合。

原型网络作为经典基线，直接在 PCA 降维后的 4 维空间中进行最近原型分类。原型网络没有训练过程，计算简单。在训练集较小时，原型向量的估计可能不够准确。随着训练集增大，原型向量更加稳定。

### 4.4.2 Omniglot 数据集结果

在 Omniglot 数据集上，5 种模型参与了比较（不包含 QCNN）。Omniglot 数据集的原始特征维度为 784 维（28×28 像素），经过 PCA 降维到 4 维后，信息损失较大。

Omniglot 数据集的样本数量比 MNIST 多得多。训练集和测试集的划分策略与 MNIST 相同，但由于总样本数量更多，每次划分的选择空间更大。

Omniglot 数据集没有运行量子卷积神经网络实验。这可能是因为 QCNN 使用 6 个量子比特，需要 6 维输入，而 PCA 降维到 6 维后信息损失更大，或者是因为 QCNN 的训练时间较长。

### 4.4.3 训练集大小对性能的影响

训练集大小从 2 增加到 40，模型表现的变化趋势反映了各模型对小样本数据的适应能力。

在训练集极小（2 个样本）的情况下，每个类别只有 1 个样本。此时原型网络的原型向量就是单个样本本身，分类完全基于最近邻。量子核方法的核矩阵很小（2×2），SVM 的决策边界可能不够稳定。

在训练集较小（5-10 个样本）的情况下，每个类别有 2-5 个样本。原型向量开始有一定的统计意义，但仍然不够稳定。量子核方法的核矩阵增大，SVM 能够学习更复杂的决策边界。

在训练集中等（20-40 个样本）的情况下，每个类别有 10-20 个样本。原型向量相对稳定，量子核方法的核矩阵足够大，SVM 的决策边界更加可靠。

### 4.4.4 重复实验与统计显著性

每个实验配置重复 10 次，目的是减少随机划分带来的方差。10 次重复实验的结果计算均值和标准差，均值反映模型的平均性能，标准差反映模型性能的稳定性。

标准差较小表示模型对训练集划分不敏感，性能稳定。标准差较大表示模型对训练集划分敏感，可能存在过拟合或欠拟合问题。

实验结果中的误差线表示标准差，可以直观地比较各模型性能的稳定性。

### 4.4.5 量子核方法与经典方法的比较

量子核方法通过量子特征映射将数据映射到高维希尔伯特空间，可能捕获经典方法难以发现的数据结构。振幅编码核和角度编码核使用不同的编码方式，ZZ 重叠核和 ZZ 投影核使用相同的编码方式但不同的核值计算方法。

原型网络作为经典基线，直接在原始特征空间（PCA 降维后）中进行分类。量子核方法在 PCA 降维后的空间上进一步进行量子特征映射，可能获得更好的分类效果。

量子卷积神经网络是端到端的量子神经网络，通过训练学习特征提取方式。与量子核方法不同，QCNN 不需要计算核矩阵，直接输出分类结果。但 QCNN 的参数数量较多，在小样本场景下可能面临过拟合问题。

### 4.4.6 不同量子编码方式的对比分析

四种量子核方法使用了三种不同的数据编码方式：振幅编码、角度编码和 ZZ Feature Map 编码。这三种编码方式在信息表达能力和电路复杂度上各有特点。

振幅编码的信息密度最高，$n$ 个量子比特可以表示 $2^n$ 维的向量。4 个量子比特的振幅编码可以处理 16 维的输入数据，编码后的量子态完整地保留了输入向量的所有信息（在归一化意义下）。振幅编码的缺点是电路深度较大，构建振幅编码电路需要 $O(2^n)$ 个门操作。在经典模拟器上，振幅编码的矩阵计算复杂度为 $O(2^{2n})$。

角度编码的信息密度较低，$n$ 个量子比特只能直接编码 $n$ 维的输入数据。4 个量子比特的角度编码只能处理 4 维的输入向量。角度编码的优点是电路简单，只需要对每个量子比特施加一个 RY 旋转门，电路深度为 $O(1)$。角度编码引入了可训练参数，通过 KTA 优化可以自适应地调整特征映射，使核矩阵更好地匹配分类任务。

ZZ Feature Map 编码的信息密度与角度编码相同，$n$ 个量子比特编码 $n$ 维输入。但 ZZ Feature Map 包含了丰富的纠缠结构，通过 CRZ 门在所有量子比特对之间施加相互作用，可以捕获特征之间的非线性关系。ZZ Feature Map 的电路深度为 $O(n^2)$，因为需要对所有 $n(n-1)/2$ 对量子比特施加 CRZ 门。

### 4.4.7 核矩阵计算复杂度分析

四种量子核方法的核矩阵计算复杂度各不相同。

振幅编码核的计算复杂度主要来自量子电路矩阵的构建和乘法。对于每个样本，振幅编码电路的矩阵大小为 $2^n \times 2^n$，矩阵乘法复杂度为 $O(2^{3n})$。核矩阵包含 $N_1 \times N_2$ 个元素，总计算复杂度为 $O(N_1 N_2 \cdot 2^{3n})$。使用并行化后，实际运行时间可以减少到原来的 $1/C$，其中 $C$ 是 CPU 核心数。

角度编码核的计算复杂度与振幅编码核类似，但电路矩阵的构建更复杂，因为包含了可训练参数的旋转门。此外，角度编码核在每次 KTA 优化迭代中都需要重新计算整个支持集的核矩阵，优化过程的总计算复杂度为 $O(T \cdot N^2 \cdot C_{circuit})$，其中 $T$ 是优化迭代次数（20 次），$N$ 是支持集大小，$C_{circuit}$ 是单次电路矩阵计算的复杂度。

ZZ 重叠核的计算复杂度主要来自量子态的模拟。对于每对样本，需要构建和执行一个包含 $O(n^2)$ 个门的量子电路，量子态向量大小为 $2^n$。核矩阵的总计算复杂度为 $O(N_1 N_2 \cdot 2^n \cdot n^2)$。ZZ 重叠核没有使用并行化，计算时间随样本数量平方增长。

ZZ 投影核的计算复杂度分为两部分：量子测量和经典计算。量子测量部分需要对每个样本执行 $3n$ 次测量（X、Y、Z 三个基各 $n$ 次），每次测量的复杂度为 $O(2^n)$。经典计算部分需要计算所有样本对之间的高斯核值，复杂度为 $O(N_1 N_2 \cdot n)$。总计算复杂度为 $O((N_1 + N_2) \cdot 3n \cdot 2^n + N_1 N_2 \cdot n)$。

### 4.4.8 实验中的数据处理细节

实验中的数据预处理对各模型的性能有重要影响。PCA 降维和 MinMaxScaler 缩放是两个关键的预处理步骤。

PCA 降维的目的是将高维数据压缩到量子比特数量对应的维度。对于 4 量子比特设置，需要将数据降维到 4 维。PCA 通过保留数据方差最大的方向来实现降维，可能会丢失一些对分类有用的信息。PCA 拟合只在训练集上进行，避免数据泄露。测试集使用训练集拟合的 PCA 变换器进行变换，保证训练和测试的一致性。

MinMaxScaler 缩放将数据缩放到 $[-1, 1]$ 区间。这个缩放对于量子编码很重要，因为量子旋转门的旋转角度通常在 $[-\pi, \pi]$ 范围内，输入数据需要在这个范围内才有意义。MinMaxScaler 的拟合也是只在训练集上进行，测试集使用训练集的缩放参数。

对于量子卷积神经网络，数据预处理不包含 MinMaxScaler 缩放。这是因为 QCNN 使用振幅编码，输入向量需要是非负的（振幅编码将输入向量直接作为量子态的振幅，振幅必须是非负实数）。PCA 降维后的数据可能包含负值，但振幅编码内部会进行归一化处理，将输入向量转换为非负的振幅分布。

### 4.4.9 SVM 分类器的使用

量子核方法使用 SVM 作为分类器，核矩阵作为预计算的核函数值。SVM 的参数设置为 `kernel="precomputed"`，表示使用预计算的核矩阵而不是内置的核函数。

SVM 的训练过程是求解一个凸优化问题，找到最优的超平面分隔两类样本。对于预计算核矩阵，SVM 直接使用核矩阵中的值计算样本之间的相似度，不需要额外的核函数评估。

SVM 的默认参数中，正则化参数 $C = 1.0$，表示对误分类样本的惩罚程度。较大的 $C$ 值会使 SVM 更倾向于正确分类所有训练样本，可能导致过拟合。较小的 $C$ 值会使 SVM 更倾向于找到更宽的间隔，可能欠拟合。实验中使用默认的 $C = 1.0$，没有进行超参数调优。

### 4.4.10 实验结果的可重复性

实验设计中采取了多种措施保证结果的可重复性。

随机种子固定为 0。数据集的类别选择、训练集和测试集的划分、角度编码参数的初始化都使用固定的随机种子。这保证了每次运行实验时，数据划分和初始条件完全相同。

重复实验次数设置为 10 次。每次重复实验使用不同的随机划分，但随机种子的序列是确定的。10 次重复实验的结果计算均值和标准差，可以反映模型在不同数据划分下的平均表现和稳定性。

实验结果保存为 CSV 文件，包含每次重复实验的详细结果。CSV 文件的列包括：训练集大小、模型类型、数据集名称、训练准确率、测试准确率。这些结果可以用于后续的统计分析和可视化。

### 4.4.11 可视化结果分析

实验结果可视化使用性能对比图，横轴为训练集大小（对数坐标），纵轴为测试准确率。图中包含 6 条曲线，分别对应 6 种模型。

对数坐标的使用是为了更好地展示训练集大小从 2 到 40 的变化趋势。在线性坐标下，2 和 5 之间的距离很小，难以区分。在对数坐标下，各个训练集大小之间的距离更加均匀，可以更清晰地观察模型在不同训练集大小下的表现。

误差线表示 10 次重复实验的标准差。误差线的长度反映了模型性能的波动范围。误差线较短表示模型性能稳定，误差线较长表示模型性能波动较大。

图中包含一条 y=0.5 的水平虚线，表示随机猜测的准确率。这是一个二分类问题，随机猜测的准确率为 50%。所有模型的准确率都应该高于这条线，否则说明模型没有学到任何有用的信息。

### 4.4.12 实验的局限性与改进方向

实验设计存在一些局限性。首先是数据集的选择，实验只使用了 MNIST Digits 和 Omniglot 两个数据集，且都进行了二分类处理。MNIST Digits 只保留了数字 0 和 1 两个类别，Omniglot 只随机选择了 2 个类别。这种二分类设置可能无法完全反映模型在多分类场景下的表现。

其次是 PCA 降维带来的信息损失。原始数据经过 PCA 降维到 4 维或 6 维后，可能会丢失一些对分类有用的信息。特别是 Omniglot 数据集，原始维度为 784 维，降维到 4 维后信息损失非常大。可以考虑使用其他降维方法（如 t-SNE、UMAP）或者增加量子比特数量来减少信息损失。

第三是量子核方法的超参数调优。实验中 SVM 的正则化参数 $C$ 使用默认值 1.0，没有进行调优。ZZ 投影核的带宽参数 $\gamma$ 使用默认值 1.0，也没有进行调优。角度编码核的 KTA 优化迭代次数设置为 20 次，可能不足以收敛。可以考虑使用交叉验证进行超参数调优。

第四是量子电路模拟的规模限制。实验中使用的是经典模拟器，量子比特数量限制在 4 到 6 个。随着量子比特数量的增加，模拟器的计算复杂度呈指数增长。在真实的量子计算机上，可以运行更大规模的量子电路，但目前的量子计算机存在噪声和误差问题。

